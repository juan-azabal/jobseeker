# === Web App Runtime Dependencies ===
# Lean subset of requirements.txt for the Docker image.
# Excludes agent-only packages (python-jobspy, pandas, etc.)
# chromadb included: api/master_cv_vectorstore.py imports it at module level

# FastAPI core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv>=1.0.1
httpx==0.28.1
authlib==1.5.2
itsdangerous==2.2.0
python-multipart==0.0.20
pyyaml>=6.0
jinja2>=3.1
ruamel.yaml>=0.18.0

# LLM providers (CV generation + onboarding profile extraction)
anthropic>=0.40.0
openai==2.21.0
posthog>=7.0.0

# Numeric (cosine similarity for skill matching)
numpy>=1.26
structlog>=24.0
asgi-correlation-id>=4.0

# CV builder + Master CV vectorstore
python-docx>=1.1.0
markdownify>=0.13
chromadb

# Geo (agent/geo.py imported by api/routes/jobs.py + api/cv/plan.py)
country-converter>=1.3
geonamescache>=1.6
Babel>=2.18
pytz>=2025.2
CurrencyConverter>=0.18
pycountry>=26.2
