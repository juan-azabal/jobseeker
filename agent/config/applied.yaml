# Global applied.yaml — legacy fallback only.
# Per-user applied data lives in config/profiles/{profile_id}-applied.yaml
# This file is no longer read by main.py when profiles have an explicit "applied:" key.
applied:
  companies: []
  ids: []
not_interested:
  ids: []
  titles: []
